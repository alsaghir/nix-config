# This file collects all overlays and exports them as a list.
[
  (import ./jetbrains-toolbox.nix)

  # When you add more overlays in the future,
  # you can just add them to this list.
  # (import ./another-overlay.nix)
]
